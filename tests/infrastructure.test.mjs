import test from 'node:test'; import assert from 'node:assert/strict'; import fs from 'node:fs';
test('canonical metadata has 66 books and 1189 chapters',()=>{const source=fs.readFileSync('data/bookMetadata.ts','utf8');assert.equal((source.match(/\['[1-3A-Z]{3}'/g)??[]).length,66);const counts=[...source.matchAll(/,([0-9]+)\]/g)].map(match=>Number(match[1]));assert.equal(counts.reduce((a,b)=>a+b,0),1189);});
test('schema includes deterministic keys, indexes and FTS5',()=>{const schema=fs.readFileSync('data/sqlite/schema.sql','utf8');assert.match(schema,/bible_versions/);assert.match(schema,/idx_verses_location/);assert.match(schema,/fts5/);});
test('reference parser normalizes accents and separators',()=>{const source=fs.readFileSync('search/referenceParser.ts','utf8');assert.match(source,/normalize\('NFD'\)/);assert.match(source,/replace\(\/\[\.:,\]\+\/g/);});
test('runtime Bible repository uses packaged SQLite, never development data',()=>{const repository=fs.readFileSync('repositories/BibleRepository.ts','utf8');const layout=fs.readFileSync('app/_layout.tsx','utf8');const dataSource=fs.readFileSync('data/sqlite/SQLiteBibleDataSource.ts','utf8');assert.doesNotMatch(repository,/developmentBibleData/);assert.match(repository,/SQLiteBibleDataSource/);assert.match(layout,/SQLiteProvider/);assert.match(layout,/assets\/bible\.db/);assert.match(layout,/Preparando sua Bíblia/);assert.match(dataSource,/counts\?\.books !== 66/);assert.match(dataSource,/counts\.verses <= 30_000/);assert.match(dataSource,/bible_verses_fts/);});
test('Expo bundles SQLite database assets',()=>{const app=JSON.parse(fs.readFileSync('app.json','utf8'));const metro=fs.readFileSync('metro.config.js','utf8');assert.ok(app.expo.assetBundlePatterns.includes('assets/**/*'));assert.match(metro,/assetExts/);assert.match(metro,/'db'/);});
test('reader keeps chapter selection and verse actions out of the reading flow',()=>{const chapters=fs.readFileSync('app/bible/[book]/index.tsx','utf8');const reader=fs.readFileSync('components/ChapterReader.tsx','utf8');assert.match(chapters,/Capítulo \{chapter\}/);assert.match(chapters,/selectorOpen/);assert.match(reader,/userBibleStorage\.toggleFavorite/);assert.match(reader,/userBibleStorage\.setHighlight/);assert.match(reader,/userBibleStorage\.saveNote/);assert.match(reader,/Share\.share/);assert.match(reader,/\$\{selected\.text\}.*\\n— \$\{reference\}/);});
test('Google auth foundation uses Supabase PKCE without committed credentials',()=>{const auth=fs.readFileSync('auth/AuthProvider.tsx','utf8');const client=fs.readFileSync('auth/supabase.ts','utf8');const example=fs.readFileSync('.env.example','utf8');assert.match(auth,/signInWithOAuth/);assert.match(auth,/exchangeCodeForSession/);assert.match(auth,/openAuthSessionAsync/);assert.match(client,/persistSession: true/);assert.match(client,/flowType: 'pkce'/);assert.match(example,/EXPO_PUBLIC_SUPABASE_URL=/);assert.match(example,/EXPO_PUBLIC_SUPABASE_ANON_KEY=/);assert.doesNotMatch(example,/https:\/\//);});
