import { Feather } from '@expo/vector-icons';
import { Pressable, StyleSheet, Text, View } from 'react-native';
import { colors, radius, spacing, typography } from '@/theme/tokens';
const actions = [{ icon: 'heart' as const, label: 'Favoritar' }, { icon: 'edit-3' as const, label: 'Grifar' }, { icon: 'share-2' as const, label: 'Compartilhar' }];
export function VerseCard() { return <View style={styles.card}><Text style={styles.verse}>O Senhor é o meu pastor; nada me faltará.</Text><Text style={styles.reference}>Salmos 23:1</Text><View style={styles.divider} /><View style={styles.actions}>{actions.map(item => <Pressable key={item.label} style={styles.action}><Feather name={item.icon} size={18} color={colors.white} /><Text style={styles.actionText}>{item.label}</Text></Pressable>)}</View></View>; }
const styles = StyleSheet.create({ card: { backgroundColor: colors.black, borderRadius: radius.lg, padding: spacing.xl, minHeight: 280, justifyContent: 'center' }, verse: { ...typography.verse, color: colors.white }, reference: { ...typography.caption, color: colors.gold, marginTop: spacing.lg }, divider: { height: 1, backgroundColor: '#303030', marginVertical: spacing.xl }, actions: { flexDirection: 'row', justifyContent: 'space-between' }, action: { alignItems: 'center', gap: spacing.sm, flex: 1 }, actionText: { ...typography.caption, color: colors.white, fontSize: 11 } });
