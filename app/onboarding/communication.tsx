import { router } from 'expo-router';
import { useState } from 'react';
import { Pressable, StyleSheet, Switch, Text, View } from 'react-native';
import { Screen } from '@/components/Screen';
import { PrimaryButton } from '@/components/PrimaryButton';
import { colors, radius, spacing, typography } from '@/theme/tokens';
export default function Communication() { const [accepted, setAccepted] = useState(false); return <Screen scroll={false} style={styles.screen}><View><Text style={styles.step}>2 de 3</Text><Text style={styles.title}>Receba uma palavra todos os dias</Text><Text style={styles.body}>Podemos enviar versículos, novidades e novos conteúdos para ajudar você a manter sua rotina de leitura.</Text><Pressable onPress={() => setAccepted(value => !value)} style={styles.option}><Text style={styles.optionText}>Quero receber versículos e novidades por e-mail</Text><Switch value={accepted} onValueChange={setAccepted} trackColor={{ false: colors.border, true: colors.gold }} thumbColor={colors.white} /></Pressable><Text style={styles.optional}>Opcional. Você poderá alterar essa escolha depois.</Text></View><PrimaryButton label="Continuar" onPress={() => router.push('/onboarding/notifications')} /></Screen>; }
const styles = StyleSheet.create({ screen: { flex: 1, justifyContent: 'space-between', paddingVertical: spacing.huge }, step: { ...typography.caption, color: colors.gold, marginBottom: spacing.xl }, title: { ...typography.display, color: colors.ink }, body: { ...typography.body, color: colors.muted, marginTop: spacing.lg }, option: { flexDirection: 'row', alignItems: 'center', gap: spacing.lg, backgroundColor: colors.surface, borderRadius: radius.md, padding: spacing.lg, marginTop: spacing.xxl }, optionText: { ...typography.body, color: colors.ink, flex: 1 }, optional: { ...typography.caption, color: colors.muted, marginTop: spacing.md } });
